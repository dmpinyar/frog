def placeBlock(choice, x, y):
    """ 
    places a block down from the given choices (probably an integer for 0, 1, or 3) and the 
    (x, y) of where it is supposed to go on the board from the top left of the block
    """
    return None
