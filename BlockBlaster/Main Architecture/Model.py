class Model:
    def __init__(self, model=None):
        """ initailizes the model and maybe gives a choice """
        self.model = model
    
    def generateActions(self, board, blocks):
        """ 
        generates the actions the model should take based on the board state and given blocks
        """
        return None
